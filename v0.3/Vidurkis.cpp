#include "Studentas.h"
#include "Vidurkis.h"
using std::vector;

double Vidurkis(vector<double> nd, double sum)
{
    double vid = 0;
    double n = nd.size();
    n = n * 1.00;
    return vid = sum / n;
};